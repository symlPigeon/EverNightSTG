[package]
name = "evernight_runtime"
version = "0.1.0"
edition = "2024"

[features]
multithread = ["dep:rayon"]

[dependencies]
evernight_core = { path = "../evernight_core" }
evernight_math = { path = "../evernight_math" }
rayon          = { version = "1", optional = true }
